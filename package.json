{
  "name": "render-ping",
  "version": "1.0.0",
  "description": "Ping service to prevent Render from timing out",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}
